{
  "name": "thp-2",
  "engines": {
    "node": "6.3.1"
  },
  "dependencies": {
    "husky": "^0.14.3"
  },
  "scripts": {
    "precommit": "rubocop && annotate",
    "prepush": "rubocop && COVERAGE=true rspec --format progress",
    "build": "cd client && npm install && npm run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  }
}
